{{-- Share this heading between the record list and the error page to match
     the other modules. It needs no record data, so it can appear when loading fails. --}}
<header class="mb-6 rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <span class="inline-flex rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold uppercase tracking-[0.14em] text-slate-600">BFAR SAAD Phase II</span>
    <h1 class="mt-3 text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">Area Management</h1>
    <p class="mt-2 max-w-3xl text-sm leading-6 text-slate-600">
        Manage municipalities and barangays used as the geographic reference layer for association registration,
        GIS filtering, monitoring reports, and coverage validation.
    </p>
</header>
